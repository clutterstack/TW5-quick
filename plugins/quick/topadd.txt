topadd

if topadd=true, put at the top. else put after conttiddler.

in quick-macros, add:

\define omni-ontop() {
"<$action-listops $tiddler=<<__omniparent__>> $subfilter='{!!savetitle} +[putfirst[]]'/>"
}
\end

\define omni-afteritem() {
"<$action-listops $tiddler=<<__omniparent__>> $subfilter='+[insertbefore:contTiddler{!!savetitle}] +[move{!!savetitle}]'/>"
}
\end


\define omni-positioner() $(position)$

(or a debugged version), 

In omnibus-macros:

 change the quickadd macrocall to include the position parameter.